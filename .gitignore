# ── Dependencies ───────────────────────────────────────
vendor/
node_modules/

# ── Nette build artefacts ──────────────────────────────
temp/
log/

# ── LOCAL CONFIG — contains DB password, never commit! ─
config/local.neon

# ── User uploads ───────────────────────────────────────
www/uploads/

# ── Vite build output (regenerated by `npm run build`) ─
www/assets/

# ── Dev / debug tools — never expose on public server ──
www/adminer.php
www/testdb.php

# ── OS / editor junk ───────────────────────────────────
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
